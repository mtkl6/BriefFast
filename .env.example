# Redis connection URL for Railway Redis instance
REDIS_URL="your-redis-connection-url"

# API key for securing endpoints (generate a strong random value in production)
API_KEY="your-random-secure-key"

# Public version for client-side (should match API_KEY)
NEXT_PUBLIC_API_KEY="your-random-secure-key"
